{
  "name": "data-agent-template",
  "version": "1.0.0",
  "private": true,
  "description": "AI 建站模板 monorepo",
  "scripts": {
    "build:packages": "pnpm -r --filter './packages/**' run build --if-present"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["core-js", "core-js-pure", "esbuild"]
  },
  "engines": {
    "node": ">= 20.0.0"
  }
}
